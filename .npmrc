# npm workspace configuration for Replit dev environment
legacy-peer-deps=true
# pnpm: prefer local workspace packages even when workspace: protocol is absent.
# This lets "*" version ranges resolve to workspace packages in both npm and pnpm.
prefer-workspace-packages=true
