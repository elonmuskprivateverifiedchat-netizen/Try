Task: Migrate the project from pnpm to npm, and clean up env-var handling in deploy scripts
ABSOLUTE BOUNDARY — UNCHANGED FROM BEFORE, READ FIRST
You must NOT modify, refactor, or touch:
	•	Any file under artifacts/*/src/routes/
	•	Any file under artifacts/*/src/controllers/ (or equivalent route-handler logic)
	•	Any admin-specific logic, admin routes, admin auth/permission checks, or admin UI behavior
	•	Any business logic governing balances, withdrawals, deposits, KYC, P2P matching, or wallet operations
This migration touches package managers and build tooling, not application logic — there is no reason this task should require editing any route, controller, or business-logic file. If you find yourself about to edit one, stop and tell me why instead of doing it.
IMPORTANT CLARIFICATION — read before doing anything
“Don’t use export” in this task means ONE specific thing only: avoid literal shell export VAR=value lines in deploy scripts (e.g. deploy.sh) for setting environment variables. It does NOT mean removing the export keyword from TypeScript/JavaScript/React source code. The ES module export/import syntax in every .ts/.tsx file is required for the codebase to function (Vite’s bundler, TypeScript’s module system, and every React component depend on it) and must be left completely untouched. Do not attempt to convert any source file from ES modules to CommonJS (require/module.exports). If anything in this task seems to require that, stop and ask me — it does not.
For environment variables, instead of shell export lines, use a .env file loaded via dotenv (for local/VPS) or the platform’s native env var dashboard (Railway, Render, Vercel already work this way and need no script changes for this). Check deploy.sh first — it already loads env vars via source .env / source /etc/xpressfx.env rather than literal export lines, so confirm that pattern is preserved through your changes rather than reintroducing export lines while rewriting things.
Step 0 — Audit before changing anything
Report back on, before touching a single file:
	1.	Every place pnpm currently appears: package.json (packageManager field, scripts), pnpm-workspace.yaml, pnpm-lock.yaml, railpack.toml, render.yaml, ecosystem.config.cjs, deploy.sh, post-merge.sh, .replit, and any script under scripts/.
	2.	Confirm the current total file count in the repo (so we can verify nothing is lost or duplicated when this is done).
	3.	List every workspace package and its current package.json (artifacts/api-server, artifacts/nextrade, artifacts/admin-portal, artifacts/mockup-sandbox, artifacts/db, lib/api-zod, lib/api-client-react, lib/api-spec, lib/db, scripts).
	4.	Flag any pnpm-specific feature in use that has no npm equivalent — in particular, pnpm-workspace.yaml currently has a minimumReleaseAgeExclude/minimum release age setting as a supply-chain security control. npm has no direct equivalent to this. Tell me explicitly what security control (if any) you plan to use to replace it — do not just silently drop it without telling me.
Step 1 — Convert workspace configuration
	•	Replace pnpm-workspace.yaml’s packages: list with an npm "workspaces" array in the root package.json (npm workspaces are defined in package.json, not a separate YAML file).
	•	npm has no catalog: feature (pnpm’s shared-version catalog). Every package currently referencing a catalog version needs that version inlined directly into its own package.json dependencies. Do this for every workspace package — do not leave any package.json with a broken/unresolved catalog reference.
	•	Delete pnpm-lock.yaml only after npm install successfully generates a new package-lock.json at the root — do not delete the old lockfile until the new one is verified to work, in case we need to roll back.
	•	Update the root package.json: remove "packageManager": "pnpm@9.15.4", add "packageManager": "npm@<version>" matching whatever npm version is actually available in this Replit environment (check with npm --version, don’t guess).
	•	Rewrite the root package.json scripts: replace pnpm --filter @workspace/api-server build with the npm equivalent (npm run build --workspace=@workspace/api-server or npm run build -w artifacts/api-server, whichever syntax actually works — verify it, don’t assume). Same for start, dev, typecheck.
	•	Replace every pnpm -r <command> (run across all workspaces) with the correct npm equivalent (npm run <command> --workspaces --if-present).
Step 2 — Update every platform config to use npm
Update each of these to use npm install / npm run instead of pnpm install / pnpm --filter, and remove any corepack prepare pnpm@... lines since they’re no longer needed:
	•	railpack.toml (Railway)
	•	render.yaml (Render)
	•	ecosystem.config.cjs (VPS / PM2 — check if it references pnpm anywhere, e.g. in a build hook)
	•	deploy.sh (VPS) — replace the pnpm install/build commands with npm equivalents. Preserve the existing .env loading pattern (via source, not export lines) exactly as it is now.
	•	vercel.json (frontends)
	•	.replit — if it references pnpm for the run command, update it
	•	scripts/predeploy.mjs — if it checks for pnpm-specific files (e.g. checking for pnpm-lock.yaml existing) or runs any pnpm command internally, update those checks to look for package-lock.json and run npm commands instead
Step 3 — Verify the build actually works under npm
Do not report this as done without showing real output:
	1.	Run npm install at the root and show the actual terminal output, including whether it succeeds or errors.
	2.	Run the build command for artifacts/api-server and confirm artifacts/api-server/dist/index.mjs is actually produced — show the real file listing, not a claim.
	3.	Run the build for each frontend package (nextrade, admin-portal, mockup-sandbox) and confirm each produces its expected output (typically a dist/ folder from Vite).
	4.	Run typecheck across the workspace and show the real output.
	5.	If anything fails, report the actual error — do not paper over it or claim success.
Step 4 — Cross-check against everything already built
Before finishing:
	•	Confirm the resilience features in artifacts/api-server/src/index.ts, src/lib/db-client.ts, and src/lib/retry.ts (graceful shutdown, crash guards, retry-with-backoff, DB pool error recovery) are untouched — these don’t involve the package manager and should not be affected by this migration at all.
	•	Confirm scripts/predeploy.mjs’s validation checks (env vars, JSON validity, conflicting build configs) still run correctly after the npm switch.
	•	Confirm the CORS/ALLOWED_ORIGINS logic in src/lib/env.ts and src/app.ts is untouched.
	•	Report the final total file count and confirm it against the count from Step 0 — explain any difference (e.g. pnpm-lock.yaml removed, package-lock.json added, any node_modules-adjacent pnpm artifacts like .pnpm folders or symlinks removed).
Final output required
	1.	Confirmation npm install/build actually succeeded, with real terminal output for each
	2.	What replaced pnpm’s minimum-release-age supply-chain protection, if anything — and if nothing replaces it, say so plainly so I can decide whether that’s acceptable
	3.	Full list of every file modified, with explicit confirmation none fall inside the restricted routes/controllers/admin/business-logic boundary
	4.	Before/after file count
	5.	Anything that didn’t translate cleanly from pnpm to npm and needed a judgment call, explained